meta {
  name: Retrieves the collection of User resources.
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: bearer
}

params:query {
  ~page: 
}

headers {
  Authorization: {{Bearer}}
}

auth:bearer {
  token: {{Bearer}}
}
